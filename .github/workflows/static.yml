<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Ajuda de Programação</title>
    <style>
        html {
            scroll-behavior: smooth; /* <-- Aqui, scroll suave */
        }
        body {
            background-color: #f4f4f4;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #333;
            color: white;
            padding: 20px;
            text-align: center;
        }
        nav {
            background-color: #333;
            padding: 10px;
            text-align: center;
        }
        nav a {
            color: white;
            margin: 0 15px;
            text-decoration: none;
            font-weight: bold;
            position: relative;
            transition: color 0.3s ease, transform 0.3s ease;
        }
        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            background-color: gold;
            left: 0;
            bottom: -5px;
            transition: width 0.3s ease;
        }
        nav a:hover {
            color: gold;
            transform: scale(1.1);
        }
        nav a:hover::after {
            width: 100%;
        }
        main {
            padding: 20px;
        }
        section {
            background-color: white;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        code {
            background-color: #eee;
            padding: 2px 5px;
            border-radius: 4px;
            font-family: monospace;
        }
        pre {
            background-color: #eee;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
        }
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px;
        }
        /* Estilo das estrelas */
        .estrelas {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 20px;
        }
        .stars {
            display: flex;
            cursor: pointer;
        }
        .star {
            font-size: 40px;
            color: gray;
            transition: color 0.2s;
        }
        .star.hover,
        .star.selecionada {
            color: gold;
        }
        .estrelas p {
            margin-top: 15px;
            font-size: 18px;
        }
    </style>
    <link rel="icon" href="favicon-32x32.jpg" type="image/x-icon">
</head>

<body>

<header>
    <h1>Ajuda de Programação</h1>
    <p>Aprenda programação de forma simples e rápida!</p>
</header>

<nav>
    <a href="#html">HTML</a>
    <a href="#css">CSS</a>
    <a href="#javascript">JavaScript</a>
    <a href="#avaliacao">Avaliação</a>
</nav>

<main>
    <section id="html">
        <h2>📄 Introdução ao HTML</h2>
        <p>HTML é a linguagem de marcação usada para criar sites. Um exemplo básico de estrutura HTML:</p>
        <pre>
&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;
    &lt;title&gt;Meu Site&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;h1&gt;Olá, mundo!&lt;/h1&gt;
  &lt;/body&gt;
&lt;/html&gt;
        </pre>
    </section>

    <section id="css">
        <h2>🎨 Introdução ao CSS</h2>
        <p>CSS serve para deixar seu site bonito! Exemplo básico de CSS:</p>
        <pre>
body {
    background-color: lightblue;
}

h1 {
    color: white;
    text-align: center;
}
        </pre>
    </section>

    <section id="javascript">
        <h2>🧠 Introdução ao JavaScript</h2>
        <p>JavaScript permite deixar seu site interativo. Exemplo de alerta:</p>
        <pre>
    &lt;button onclick="alert('Olá, mundo!')"&gt;
    Clique Aqui
    &lt;/button&gt;
        </pre>

        <h3>🎯 Teste aqui:</h3>
        <button onclick="alert('Olá, mundo!')">Clique Aqui</button>
    </section>

    <section class="estrelas" id="avaliacao">
        <h2>⭐ Avalie Nosso Site</h2>
        <div class="stars">
            <span class="star" data-value="1">&#9733;</span>
            <span class="star" data-value="2">&#9733;</span>
            <span class="star" data-value="3">&#9733;</span>
            <span class="star" data-value="4">&#9733;</span>
            <span class="star" data-value="5">&#9733;</span>
        </div>
        <p id="mensagem">Nenhuma avaliação ainda.</p>
    </section>
</main>

<footer>
    &copy; 2025 - Ajuda de Programação
</footer>

<script>
    const stars = document.querySelectorAll('.star');
    const mensagem = document.getElementById('mensagem');

    let selecionado = 0;

    stars.forEach((star) => {
        star.addEventListener('mouseover', () => {
            const valor = star.getAttribute('data-value');
            highlightStars(valor);
        });

        star.addEventListener('mouseout', () => {
            highlightStars(selecionado);
        });

        star.addEventListener('click', () => {
            selecionado = star.getAttribute('data-value');
            mensagem.textContent = `Você avaliou com ${selecionado} estrela${selecionado > 1 ? 's' : ''}!`;
        });
    });

    function highlightStars(valor) {
        stars.forEach((star) => {
            if (star.getAttribute('data-value') <= valor) {
                star.classList.add('hover');
            } else {
                star.classList.remove('hover');
            }
        });
    }
</script>

</body>
</html>
